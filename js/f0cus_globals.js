//global variables
var
    PAPER_HEIGHT = 2000,
    PAPER_WIDTH = Math.round(Math.sqrt(2) * PAPER_HEIGHT),
    RECT_HEIGHT = 60,
    RECT_WIDTH = 80,
    ID_COUNTER = 0;


//drawing variables
var
    DRAW_RECT = false;
    SELECT_ELEM = false;
    JSON_NODES = [];
