//global variables
var
    PAPER_HEIGHT = 2000,
    PAPER_WIDTH = Math.round(Math.sqrt(2) * PAPER_HEIGHT),
    IDF_HEIGHT = 40,
    IDF_WIDTH = 60,
    IDF_Xmult = 150,
    IDF_Ymult = 60,
    ID_COUNTER = 0;


//drawing variables
var
    DRAW_RECT = false;
